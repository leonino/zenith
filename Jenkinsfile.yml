pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'mvn pacacke -Dmaven.test.skip=true'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh 'mvn test'
            }
        }
    }
}