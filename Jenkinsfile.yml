pipeline {
    agent any
    stages {
        stage('Build') {
            agent {
                label 'maven'
            }
            steps {
                sh 'mvn pacacke'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh 'mvn test'
            }
        }
    }
}